// postcss.config.js

const config = {
  plugins: {
    '@tailwindcss/postcss': {}, // Este é o nome correto que o erro pediu      // Mantenha este para compatibilidade
  },
};

export default config;